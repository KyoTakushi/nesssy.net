import React from "react"
import { Link } from "gatsby"

export default () => (
  <header className="fixed-header">
    <Link to="/">
      <div className="fixed-header__text kaku">Neo Testament <span>ver.1</span></div>
    </Link>
  </header>
)
